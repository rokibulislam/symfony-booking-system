{{ form_start(roomtypeForm) }}

    <div class="form-group">
        <div class="col-md-12 mb-3">
            {{ form_row(roomtypeForm.name, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-12 mb-3">
            <button class="btn {{ button_color|default('btn-outline-primary') }}  mt-3">{{ button_label|default('Save Room Type') }}</button>
        </div>
    </div>

{{ form_end(roomtypeForm) }}